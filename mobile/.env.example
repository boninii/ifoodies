# URL base da API Laravel da cantina.
# Copie este arquivo para `.env` e ajuste se necessário.
# Variáveis com prefixo EXPO_PUBLIC_ ficam disponíveis no app via process.env.
EXPO_PUBLIC_API_URL=https://cantinaapi.dingols.com.br/api/cantina
